version: "3.8"
services:
  medi-pharma:
    build: .
    ports:
      - "8095:8095"
    environment:
      - MIMO_API_KEY=${MIMO_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
